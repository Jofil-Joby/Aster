export function startClock(state){const update=()=>{const d=new Date(),h=d.getHours();document.querySelector('#clock').textContent=d.toLocaleTimeString([],{hour:'numeric',minute:'2-digit'});document.querySelector('#date').textContent=d.toLocaleDateString([],{weekday:'long',month:'long',day:'numeric'});const part=h<5?'Night':h<12?'Morning':h<17?'Afternoon':'Evening';document.querySelector('#greeting').textContent=`GOOD ${part.toUpperCase()}${state.settings.name?`, ${state.settings.name.toUpperCase()}`:''}`};update();setInterval(update,1000);return update}
export function context(state){const active=state.tasks.filter(t=>!t.done).length,total=state.tasks.length,today=state.today.length,sessions=state.timerState.sessionsToday||0;let text=total===0?'Nothing planned yet.':active===0?'Everything planned is done.':active>5?'Busy day ahead.':`${active} task${active===1?'':'s'} · ${today} thing${today===1?'':'s'} today`;if(sessions)text+=` · ${sessions} focus session${sessions===1?'':'s'}`;document.querySelector('#daily-context').textContent=text}
